version: '3.8'

services:
  app:
    image: ubuntu
    environment:
      WAIT_HOSTS: kafka:9092
    working_dir: /opt
    volumes:
        - type: bind
          source: .
          target: /shared_1

      #- ../test/test-conf-file.conf:/opt/test/test-conf-file.conf
    command: 
        - /bin/bash
        - -c
        - |
          ls -laR /shared_1
