version: '3.8'

services:
  app:
    image: ubuntu
    environment:
      WAIT_HOSTS: kafka:9092
    working_dir: /opt
    volumes:
        - type: bind
          source: .
          target: /shared_1

    command: 
        - /bin/bash
        - -c
        - |
          ls -laR /shared_1
